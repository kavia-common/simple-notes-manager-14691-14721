# PUBLIC_INTERFACE
# Frontend configuration for Notes app
# Set the backend API base URL exposed to the browser (e.g., https://your-backend-host)
# If omitted, the app will use /api (assumes reverse proxy configured to backend).
PUBLIC_NOTES_API_BASE=https://backend.example.com
